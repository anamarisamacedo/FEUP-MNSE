import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#F4F2F3',
    },
    secondary: {
      main: '#A7C6DA',
    },
    background: {
      default: '#F49F0A',
    },
    text: {
      primary: '#FFFFFF',
      secondary: '#06070E',
    },
  },
});

export default theme;
